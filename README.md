# Mesh -- Web Developer & Data Scientist (Portfolio)

Development scaffold with Vite + React + TypeScript + Tailwind + Framer Motion

Install and run:

```bash
npm install
npm run dev
```

Notes:

- Site root background is #0C0C0C and uses Kanit font.
- Main components live in `src/sections` and `src/components`.
